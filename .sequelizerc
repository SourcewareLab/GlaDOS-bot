const path = require('path');

const rootDir = path.resolve(__dirname);

const distDataDir = path.join(rootDir, "dist", "data");
const srcDataDir = path.join(rootDir, "src", "data");

module.exports = {
  config: path.join(distDataDir, "config", "database.config.ts"),
   'models-path': path.join(distDataDir, "models"),
   'migrations-path': path.join(srcDataDir, "migrations"),
};